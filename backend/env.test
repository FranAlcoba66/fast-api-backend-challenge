# Test Environment Configuration
APP_NAME=backend
APP_ENV=testing
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Main Database Configuration (not used in tests)
DATABASE_URL=postgresql://user:password@db:5432/challenge
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=challenge

# Test Database Configuration
TEST_DATABASE_URL=postgresql://test_user:test_password@test_db:5432/test_challenge
TEST_POSTGRES_USER=test_user
TEST_POSTGRES_PASSWORD=test_password
TEST_POSTGRES_DB=test_challenge

# API Configuration
PROJECT_NAME=Clean Architecture FastAPI Demo
VERSION=1.0.0
API_V1_STR=/api/v1

# Security Configuration
SECRET_KEY=test-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Pool Settings (reduced for testing)
DB_POOL_SIZE=1
DB_MAX_OVERFLOW=0
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# External API Configuration
POKEMON_API_URL=https://pokeapi.co/api/v2

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]
ALLOWED_ORIGINS=http://localhost:3000
